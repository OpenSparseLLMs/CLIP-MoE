from setuptools import setup, find_packages

setup(
    name='CLIPMOE',  # 你的包名
    version='0.1.0',  # 版本号
    packages=find_packages(),  # 自动找到项目中的所有包
    # install_requires=[
    #     # 这里填写项目依赖的其他包
    # ],
)